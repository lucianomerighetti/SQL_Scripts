-- objetos que tenha redaction aplicado
SELECT *
FROM REDACTION_POLICIES
WHERE OBJECT_OWNER = 
AND OBJECT_NAME = ;

-- colunas com redaction aplicado
SELECT *
FROM REDACTION_COLUMNS
WHERE OBJECT_OWNER = 
AND OBJECT_NAME = ;